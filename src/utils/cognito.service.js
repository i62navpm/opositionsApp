const debug = require('debug')('utils:cognitoSDK')
// const AWSCognito = require('aws-sdk/clients/cognitoidentity')

export default class AWSCognitoSDK {
  constructor () {
    debug('Init AWSCognitoSDK')
  }
}

